import React from 'react';
import './heroSection.css';

const HeroSection = () => {
    return (
        <section className="hero-section">
            <div id="hero-container" className="hero-section__container">
               
               
 
            </div>
        </section>
    );
};

export default HeroSection;
